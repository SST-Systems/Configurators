namespace SST.Configurators
{
    /// <summary>
    /// Marker interface for a module processor - the serializable object that holds a module's configured
    /// entries and drives their execution or evaluation at runtime.
    /// </summary>
    public interface IConfiguratorProcessor { }
}